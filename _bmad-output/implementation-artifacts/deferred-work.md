# Deferred Work

Items deferred from code reviews and other workflows: real, but not actionable
at the time they were raised. Each entry records why.

## Deferred from: code review of 1-1-the-full-gate-runs-in-ci-through-one-invocation (2026-08-16)

- **`sonar-project.properties` comment names a nonexistent task `pixi run cov`** (real task: `test-cov`) [sonar-project.properties:20]. Pre-existing, not touched by the reviewed diff — though it sits directly in the coverage.xml pipeline that diff modifies at both ends (producer in `ci.yml`, consumer in `sonarqube.yml`). Worth fixing opportunistically next time either file is touched.
- **Tests never exercise `pixi`'s runtime fail-fast `depends-on` behavior**, only the static manifest [tests/unit/test_gate_contract.py]. Out of Story 1.1's stated unit-test scope ("no I/O beyond reading repository files, no network, no database"); the sequential/fail-fast/exit-code-propagation behavior was verified manually in a scratch workspace and recorded in the story's Debug Log References. If pixi's `depends-on` scheduling semantics ever change, nothing here would catch the regression — would need an integration-level check that intentionally fails an early gate step and asserts a later one never ran.
- **`_invokes()` is a string-matcher fragile to `pixi run -e dev <task>`, chained (`&&`), or piped invocations** [tests/unit/test_gate_contract.py:74]. No current workflow invokes tasks this way, so zero present impact. Hardening backlog if invocation styles diversify.
- **Contract-test blind spots with no current instances** [tests/unit/test_gate_contract.py]: reusable `uses:` workflow calls (job-level references to another local workflow file), target-scoped pixi tasks (`[target.<platform>.tasks]`), duplicate top-level YAML keys (PyYAML silently keeps the last occurrence), and artifact-retention-expiry error-message clarity (a re-run past `retention-days: 7` produces a generic "not found" indistinguishable from "never produced"). None apply to the current five workflow files or task table. Hardening backlog.

## Deferred from: code review of 1-2-the-gate-runs-against-postgresql (2026-08-15)

- source_spec: `1-2-the-gate-runs-against-postgresql.md`
  summary: Nothing type-checks `tests/` — mypy is scoped to `src/` in both the `typecheck` task and the pre-commit hook, so annotation errors in the suite are invisible to the gate.
  evidence: `pixi.toml:197` defines `typecheck = "mypy src/"`, and `.pre-commit-config.yaml`'s mypy hook also runs `mypy src/` with `pass_filenames: false`, so no configuration reaches the tests directory. This review found a missing return annotation on a fixture in the new `tests/unit/test_database_selection.py` that no gate step would ever have reported (patched here by hand). Pre-existing and project-wide rather than caused by this story; widening the scope would surface an unknown backlog across the whole existing suite, which is its own piece of work.

- source_spec: `1-2-the-gate-runs-against-postgresql.md`
  summary: Importing `config.settings.base` calls the process-global `configure_structlog()`, so every settings-reload test silently reconfigures logging for whatever runs after it.
  evidence: `src/config/settings/base.py` calls `configure_structlog()` at import time. `tests/unit/test_settings.py` established the evict-and-reimport pattern and this story's `test_database_selection.py` extends it, so the module is now re-imported roughly a dozen times per run — each time re-applying a non-test logging configuration ahead of `test_observability_logging.py` and `test_request_logging.py`. No ordering failure is observed today, and the tests pass in any order currently produced, but the coupling is real and invisible. The fix belongs with the observability work rather than with a database story.
  update (2026-08-17): **this went live during Story 2.6** — the predicted ordering failure arrived, from a new re-importer in `tests/integration/` rather than from the unit files named above. It took down three of Story 2.5's `capture_logs()` assertions, and it failed in the shape this entry did not anticipate: silently, as `len(events) == 0` in an unrelated file, rather than as a logging misconfiguration. `tests/integration/test_site_migration.py` now saves and restores `structlog.get_config()` around its re-import, which closes that one call site. The general fix — a save/restore helper beside `configure_structlog()` that every re-importer uses, instead of each one remembering — is still open and still belongs with the observability work. See the Story 2.6 entry below for the remaining call site.

- source_spec: `1-2-the-gate-runs-against-postgresql.md`
  summary: `ATOMIC_REQUESTS` is applied to the `default` alias only, so AD-9's forecast second database would be served non-atomically without anything in the source noticing.
  evidence: `src/config/settings/base.py:80` sets `DATABASES["default"]["ATOMIC_REQUESTS"] = True` against one hardcoded key, while AD-9 states that Epic 9's refusals "iterate every configured database" — the architecture already expects more than one alias. This story's `test_every_branch_sets_atomic_requests` asserts the setting across *every* configured alias and names the offenders, so the day a second database is added the suite reports which one is missing it; but the assertion is currently vacuous beyond `default`, and the fix is a one-line loop in `base.py`. Not made here: the story's own Task 4 forbids touching `base.py`, and Dev Notes state "do not add a second database in this story". Belongs with the Epic 9 work that introduces the second alias.

### DW-1: Follow-up review still recommended for 1-2-the-gate-runs-against-postgresql after the damping cap was spent
origin: review-budget-followup
source_spec: `1-2-the-gate-runs-against-postgresql.md`
location: n/a
severity: low
reason: The follow-up-review damping cap (limits.max_followup_reviews = 1) was spent with the story finalized (status: done, verify green) while the review pass still recommended an independent follow-up. The work was committed by bmad-loop run 20260815-155824-9f6b; this entry preserves the lingering recommendation for a deliberate later review.
status: open

- source_spec: `1-4-no-network-surface-exists-beneath-djangos-routing.md`
  summary: WhiteNoise answers requests for collected static assets from middleware, below the URL resolver, so Epic 4's route allowlist cannot see that surface even after this story removes the websocket handler.
  evidence: `src/config/settings/base.py:167` installs `whitenoise.middleware.WhiteNoiseMiddleware`. Its `__call__` looks the request path up in its own manifest and returns `self.serve(...)` on a hit without ever calling `get_response`, so those responses never reach the URLconf and cannot be named by view callable — which is the mechanism AD-26 and FR-17 rely on. `epics.md:237` makes this story a precondition for Epic 4's allowlist being "complete rather than merely present"; the websocket half is now closed, the static half is not. Pre-existing (WhiteNoise predates this story) and not fixable here: deciding whether the allowlist must account for middleware-level file serving, or whether static assets are declared out of scope, is Epic 4 Story 4.6's call. This story's `docs/development.md` section now names the exception explicitly so 4.6 cannot inherit the unqualified claim.

- source_spec: `1-4-no-network-surface-exists-beneath-djangos-routing.md`
  summary: No test pins `MIDDLEWARE`, so a middleware that answers below the URL resolver can be added without anything failing — the docs section's list of below-resolver surfaces is prose that can silently go stale.
  evidence: The follow-up review found that the section's original "one known exception" claim was already short by two: `django.middleware.security.SecurityMiddleware` returns an SSL redirect from `process_request` (with `SECURE_SSL_REDIRECT` on in `production.py:51`) and `corsheaders.middleware.CorsMiddleware.__call__` returns `check_preflight(request)` without calling `get_response` (verified against the installed source). The section was corrected and `tests/unit/test_asgi_surface.py::TestTheForwardRuleIsDocumented` now asserts it names all three, but that test reads the *docs*, not `MIDDLEWARE` — adding a fourth short-circuiting middleware still fails nothing. `grep -rn "MIDDLEWARE" tests/` returns only `tests/unit/test_settings.py:61,72`, which assert debug_toolbar's presence in `local.MIDDLEWARE`; the base list is unpinned. Pre-existing and not fixable here: pinning the middleware chain is an assertion about what the route allowlist must account for, which is Epic 4 Story 4.6's decision alongside the WhiteNoise entry above.

- source_spec: `1-4-no-network-surface-exists-beneath-djangos-routing.md`
  summary: `src/config/wsgi.py`'s inherited docstring recommends replacing Django's WSGI application with a custom one that delegates to it — the exact shape AD-16 deletes on the ASGI side — and nothing pins what that module binds.
  evidence: `src/config/wsgi.py:9-13` reads "it also might make sense to replace the whole Django WSGI application with a custom one that later delegates to the Django one. For example, you could introduce WSGI middleware here". That is inherited cookiecutter boilerplate, pre-existing and untouched by this story, but it is advice to reintroduce a surface beneath the router in the twin entrypoint — and `src/config/settings/base.py:89` declares `WSGI_APPLICATION = "config.wsgi.application"` while no `ASGI_APPLICATION` setting exists at all, so `wsgi.py` is the module Django's own `runserver` resolves. Story 1.4's assertions cover `asgi.py` only: `tests/unit/test_asgi_surface.py` reads `src/config/asgi.py` and nothing asserts that `wsgi.py` binds an unwrapped `get_wsgi_application()`. Not fixed here because AD-16 is written about `asgi.py` and Stories 1.5 and 1.6 both touch these entrypoints; extending the AD-16 surface assertions to the WSGI twin, and deleting the docstring's invitation, belongs with whichever of those closes the entrypoint pair.

- source_spec: `1-4-no-network-surface-exists-beneath-djangos-routing.md`
  summary: The two deployment entrypoints disagree about their default settings module — `asgi.py` falls back to `config.settings.local` (DEBUG=True) while `wsgi.py` falls back to `config.settings.production` — so `pixi run serve`, described as "production-like ASGI", runs in DEBUG when the environment does not set `DJANGO_SETTINGS_MODULE`.
  evidence: `src/config/asgi.py:23` is `os.environ.setdefault("DJANGO_SETTINGS_MODULE", "config.settings.local")`; `src/config/wsgi.py:27` sets the same variable to `config.settings.production`; `src/config/settings/local.py:9` is `DEBUG = True`. `pixi.toml:190` describes `serve` as "Serve with uvicorn (production-like ASGI)". Under that fallback `src/config/urls.py`'s `settings.DEBUG` block also adds `staticfiles_urlpatterns()`, so the served URLconf differs from production's. AD-16's own claim is unaffected — the DEBUG-only static patterns are routes, visible to the resolver and therefore to Epic 4's allowlist — which is why this is recorded rather than fixed. Pre-existing: the diff does not touch either line, and neither prior review pass recorded it. Deciding whether the ASGI entrypoint should default to production, or whether `serve`'s description should stop claiming production parity, is the process-model question Story 5.2 owns.
  update (2026-08-28): **resolved by Story 5.2.** The description was the wrong half to leave standing: `serve` is now described as the cross-platform *local* ASGI server, invoked as `pixi run -e dev serve`, with the reasoning beside it in `pixi.toml` and the same correction made to `docs/development.md` and `docs/deployment.md`. The entrypoint default is deliberately unchanged -- `manage.py` carries the identical `config.settings.local` fallback, so moving `asgi.py`'s alone would be half a fix, and stage-1 condition 1 (`src/config/startup/stage_one.py`) already refuses a deployed process that loaded the local settings module, so the fallback fails closed at settings import. `docs/deployment.md` now states what a platform that omits `DJANGO_SETTINGS_MODULE` gets.

### DW-2: Follow-up review still recommended for 1-4-no-network-surface-exists-beneath-djangos-routing after the damping cap was spent
origin: review-budget-followup
source_spec: `1-4-no-network-surface-exists-beneath-djangos-routing.md`
location: n/a
severity: low
reason: The follow-up-review damping cap (limits.max_followup_reviews = 1) was spent with the story finalized (status: done, verify green) while the review pass still recommended an independent follow-up. The work was committed by bmad-loop run 20260815-185051-bd34; this entry preserves the lingering recommendation for a deliberate later review.
status: open

## Deferred from: code review of 1-5-the-coverage-floor-is-one-constant-and-its-measurement-is-closed (2026-08-16)

- source_spec: `1-5-the-coverage-floor-is-one-constant-and-its-measurement-is-closed.md`
  summary: Story 1.2's no-dodging guard scans only `test_*.py` and `conftest.py`, so a `pytest.skip` moved into a helper module escapes it — and this story now ships the first worked example of exactly that move.
  evidence: `tests/unit/test_suite_policy.py:64` defines `_test_modules()` as `TESTS_ROOT.rglob("test_*.py") + TESTS_ROOT.rglob("conftest.py")`, which matches neither `tests/coverage_policy.py` nor the pre-existing `tests/factories.py`. Story 1.5's Task 1 mandates a `pytest.skip` for the no-coverage-session case, which collides head-on with that ban, so the skip (and, after the review pass, a `pytest.fail`) live in `tests/coverage_policy.py:391-431` — outside the scan by construction, with the reasoning stated openly in the function's docstring. The placement is defensible on its merits (it guards coverage never having been requested, not an inconvenient backend, and the review pass narrowed it further so `--no-cov` now fails rather than skips), but the guard is now demonstrably opt-out: a future PostgreSQL skip can take the same route. Not fixed here because the repair is a change to `tests/unit/test_suite_policy.py` — widening `_test_modules()` to `rglob("*.py")` and registering a named, justified exemption for `require_coverage_session` — which is a file this story does not own, and narrowing versus exempting is a decision about Story 1.2's guard rather than about the coverage floor.

- source_spec: `1-5-the-coverage-floor-is-one-constant-and-its-measurement-is-closed.md`
  summary: `tests/unit/test_gate_contract.py` still reads pixi tasks at fixed table paths, so its gate-shape assertions cannot see a task declared in a target-scoped table — the same blind spot this story just closed in its own reader.
  evidence: The 1-1 deferred entry above already records "target-scoped pixi tasks (`[target.<platform>.tasks]`)" as a contract-test blind spot with "no current instances". This review demonstrated it is live rather than theoretical: a task injected at `[target.osx-arm64.tasks]` carrying `--cov-fail-under=50` was invisible to the fixed-path reader, and `pixi.toml:92,96` already use `[target.<platform>.dependencies]`, so the idiom is established in this manifest. Story 1.5 fixed it for the coverage floor by giving `tests/coverage_policy.py:219` a recursive walk (the third such walk in the suite, after `tests/unit/test_asgi_surface.py:162`), and `tests/unit/test_gate_contract.py:100` is now the last fixed-path reader left. Not fixed here because `test_gate_contract.py` is Story 1.1's file and its assertions are about the gate's shape rather than the coverage floor; the repair is to have all three call one walk, which is a consolidation better done deliberately than as a side effect of a coverage story.

## Deferred from: code review of 1-6-import-roots-are-declared-in-exactly-one-place (2026-08-16)

- source_spec: `1-6-import-roots-are-declared-in-exactly-one-place.md`
  summary: AD-7's stated plan for Epic 9 — appending `src/django_apps/` to the same `sources` array — does not produce the unqualified app imports AD-6 promises, because hatchling applies the first matching path prefix and `src` shadows `src/django_apps`.
  evidence: Hatchling sorts the `sources` entries and returns on the first prefix that matches when computing a file's distribution path, so with `sources = ["src", "src/django_apps"]` a file at `src/django_apps/billing/models.py` is matched by `src` first and lands at `django_apps/billing/models.py` in the wheel — importable as `django_apps.billing`, not `billing`. AD-6 requires the opposite: "An app at `src/django_apps/billing/` is imported and installed as `billing`, unqualified." AD-7 asserts the retained site "declares both roots via a `sources` remapping of `src/` and `src/django_apps/`" and `epics.md:347` records that Story 1.6's single site "gains the `src/django_apps/` root in Epic 9 without gaining a second site"; both assume an addition that does not behave as described. Not caused by this story — `src/django_apps/` does not exist and Story 1.6 declares `src/` alone — but it invalidates the assumption Story 9.2 will act on, and the failure mode is silent: the wheel builds and the suite passes while every app's import path is wrong. A warning is recorded in the comment at the declaration site in `pyproject.toml`. Story 9.2 must verify the two-root remapping against a built wheel rather than assuming it, and if hatchling cannot express both roots in one `sources` table, AD-7's "one site" claim needs a mechanism that can.

## Deferred from: code review of 1-7-no-third-party-package-resolves-from-the-package-index (2026-08-16)

- source_spec: `1-7-no-third-party-package-resolves-from-the-package-index.md`
  summary: Nothing makes `pixi.lock` authoritative during verification, so a stale committed lock is invisible to the gate and this story's lock-pinning assertions run against a lock pixi may have just re-solved.
  evidence: Story 1.7's `test_lock_file_resolves_every_declared_dependency` asserts that every declared dependency resolves to a concrete version satisfying its specifier, which is NFR-5's "the same lock file produces the same component". But no task or workflow passes `--locked` or `--frozen`: `pixi.toml`'s `ci` chain invokes `precommit`/`build`/`typecheck`/`lint`/`test-cov` with no such flag, and `.github/workflows/ci.yml` uses `prefix-dev/setup-pixi` without setting its `locked:` input. `pixi run ci` therefore re-solves and may rewrite `pixi.lock` before the tests read it, so the assertion is made against whatever pixi just produced rather than against what is committed. The review pass closed the adjacent half of this — the lock must now agree with the manifest's declared ranges, which catches a manifest edited without re-solving — but it cannot catch a lock that pixi silently refreshed during the same run. Not fixed here because the repair is a change to the gate's own shape (`ci`'s task list and `setup-pixi`'s inputs), which Story 1.1 owns and `tests/unit/test_gate_contract.py` asserts; adding a flag to the gate as a side effect of a supply-chain story would put the change outside the test that guards it.

- source_spec: `1-7-no-third-party-package-resolves-from-the-package-index.md`
  summary: The `docs` task is not part of the `ci` chain, so no gate step builds the documentation and a broken cross-document link fails nothing.
  evidence: `pixi.toml` defines `docs = { cmd = "mkdocs build --strict" }` but `ci`'s `depends-on` is `[precommit, build, typecheck, lint, test-cov]`, and no file under `.github/workflows/` invokes mkdocs. This story added a cross-document reference — `docs/observability.md` now links `[Supply chain](development.md#supply-chain)` — which resolves today (verified by running `pixi run docs`, which builds clean under `--strict`) but is unguarded: renaming the heading breaks the link silently. `mkdocs.yml` lists both pages in `nav`, so the material for the check exists. Pre-existing rather than caused by this story — the gate has never built the docs — and the repair is one entry in `ci`'s `depends-on`, which is Story 1.1's file and is asserted by `tests/unit/test_gate_contract.py`.

## Deferred from: code review of 1-9-django-runs-on-the-lts-release (2026-08-16)

- source_spec: `1-9-django-runs-on-the-lts-release.md`
  summary: Five forward-looking planning documents still name Django 6.0 as the pinned stack version, and one of them instructs a future agent to trust that value without checking it.
  evidence: AC #4 scoped this story's document amendments to `epics.md` and `ARCHITECTURE-SPINE.md`, and both were amended. Five other forward-looking sites were left naming 6.0: `_bmad-output/specs/spec-django-15-factor-base/SPEC.md:70,106` ("the pinned Django 6.0"), `_bmad-output/specs/spec-django-15-factor-base/capability-map.md:30` ("unproven against Django 6.0"), `7-5-object-storage-attaches-an-s3-compatible-backend-with-a-local-substitution.md:57,97,153`, and `8-1-six-pre-locked-environments-come-from-one-lock-file.md:106`. The 7.5 site is the dangerous one: its block is headed "**Stack, authoritative — do not web-search for versions**" and names Django 6.0, so a future dev-auto run implementing object storage would build against a version this project no longer pins, under an explicit instruction not to verify it. No test in this repo reads a planning document — `grep -rn '_bmad-output' tests/` returns nothing — so the drift is invisible to the gate; `docs/development.md` is the only prose file under any assertion. Not fixed here because rewriting the Dev Notes of unrun story specs is a larger act than an AC #4 amendment and changes what those stories will do; whoever picks up 7.5 or 8.1 must reconcile the stack block first.

- source_spec: `1-9-django-runs-on-the-lts-release.md`
  summary: `djangorestframework-stubs` had to drop two minors to `3.16.9` to satisfy the 5.2 stub line, so strict mypy now checks `djangorestframework 3.18.0` call sites against 3.16 stubs — the same stub/runtime misalignment this story exists to close, reopened one package over.
  evidence: The solve is forced, not chosen: `djangorestframework-stubs` 3.17.0/3.17.1 require `django-stubs >=6.0.4` and 3.18.0 requires `>=6.0.9`, none of which is satisfiable at `django-stubs >=5.2,<5.3`. The full conflict output is in this story's Debug Log References, and the reasoning is recorded at `pixi.toml:161-167`. Story 1.3 made `typecheck` a gate condition under `strict`, and this story added `test_the_declared_stubs_track_the_runtime_series` for the Django pair plus `django-stubs-ext`; no equivalent assertion exists for the DRF pair, because one would fail today with no available fix. Not fixable in this story: it resolves only when `djangorestframework-stubs` publishes a release built against the `django-stubs` 5.2 line, or when the project moves to the next LTS (6.2), whichever comes first. Whoever moves the pin should add the DRF pair to `STUB_PACKAGES`-style alignment at that point.

- source_spec: `1-9-django-runs-on-the-lts-release.md`
  summary: Nothing turns Django deprecation warnings into failures, so the backlog that the eventual 6.2 LTS move must clear accumulates silently on 5.2.
  evidence: `pyproject.toml`'s `[tool.pytest.ini_options]` sets no `filterwarnings` entry promoting `RemovedInDjango60Warning`/`RemovedInDjango61Warning` to errors, and no task runs `django-upgrade`. The move this story just performed went 6.0 -> 5.2, so code written against 5.2 can now use APIs that 6.0 already removed and nothing objects; `pixi run ci` reports 32 warnings and passes. `pixi.toml:36-37` writes the exit condition down as "6.2, April 2027", which makes the size of that move a thing worth bounding now rather than discovering then. Pre-existing rather than caused by this story — no deprecation gate has ever existed — but the LTS pin is what makes it consequential, since an LTS series is where a project sits long enough for a backlog to build. The repair is one `filterwarnings` entry plus a decision about whether third-party deprecations (allauth, DRF, celery-beat) are in scope, which is a gate-shape change Story 1.1 owns.

## Deferred from: code review of 2-1-the-user-model-carries-the-identity-key (2026-08-16)

- source_spec: `2-1-the-user-model-carries-the-identity-key.md`
  summary: `--reuse-db` means a new migration never reaches a developer's existing test database, so the first run after any schema story fails with a confusing "no such column" that CI can never reproduce.
  evidence: `pyproject.toml`'s `[tool.pytest.ini_options] addopts` carries `--reuse-db`, and no pixi task or workflow passes `--create-db`. pytest-django reuses the stored test database without re-running migrations, so this story's `0002_user_idp_subject` is absent from any test database created before it. CI builds a fresh database each run and is therefore blind to it. Pre-existing rather than caused by this story — it applies to every migration the project will ever add — and the repair is a documented `test-recreate`-style task or a note in `docs/development.md`, which is a gate/workflow shape change Story 1.1 owns rather than a schema story's business.

- source_spec: `2-1-the-user-model-carries-the-identity-key.md`
  summary: No gate step runs `makemigrations --check --dry-run`, so a model edited without a matching migration passes the whole gate and fails only at deploy.
  evidence: `grep` over `pixi.toml` and `.github/workflows/` finds no `--check` invocation; `ci`'s `depends-on` is `[precommit, build, typecheck, lint, test-cov]`. The test suite migrates from scratch each CI run, so model/schema drift is invisible: the schema is rebuilt from the migrations, and a model field with no migration simply never reaches a column. This story added the project's first non-initial migration, which is what makes the missing guard consequential, but the drift hole predates it. The repair is one entry in the gate's task chain, which `tests/unit/test_gate_contract.py` asserts and Story 1.1 owns.

- source_spec: `2-1-the-user-model-carries-the-identity-key.md`
  summary: `UserAdminChangeForm` inherits `Meta.fields = "__all__"`, so any consumer importing the form directly — it is public API under AD-5 — gets an editable identity key even though the admin does not.
  evidence: `src/django_service/users/forms.py` declares `UserAdminChangeForm(admin_forms.UserChangeForm)` and Django's `UserChangeForm.Meta` sets `fields = "__all__"`; instantiating the form yields an `idp_subject` field (`required=False`). The admin change view is safe only because `ModelAdmin.get_form` subtracts `readonly_fields` — verified here, and now pinned by `test_the_change_form_carries_no_editable_identity_key` and `test_the_change_view_ignores_a_posted_identity_key` in `tests/integration/users/test_admin.py`. Not fixed here because Task 3 states "do not add it to `UserAdminChangeForm`", and narrowing that form's `Meta` (via `exclude` or an explicit field list) is a change to the form's own contract rather than to this story's field. Whoever owns the allauth/admin surface in Story 2.6 should decide whether the public form must exclude the identity key independently of the admin.

- source_spec: `2-1-the-user-model-carries-the-identity-key.md`
  summary: `""` is a second representation of "no identity key" that the schema accepts once and rejects the second time, so a mapper that writes an empty subject fails mid-authentication rather than at the write that was wrong.
  evidence: The field is `CharField(null=True, blank=True)` with no `CheckConstraint`, no `clean()` normalization and no test covering the empty string. `null=True` makes repeated NULLs legal, but `""` is a value: the first user stored with `idp_subject=""` succeeds and the second raises `IntegrityError` on the unique constraint — the exact "same person resolving to two users / collidable claim" failure mode AD-11 exists to prevent, reached by a different route. The admin form is not the exposure (`empty_value` resolves to `None`); direct model writes and the Story 2.4 mapper are. Not fixed here because the Dev Notes scope this story to "the schema change and nothing more" and forbid additional constraints; Story 2.4 must either normalize `""` to `None` before writing or the model must grow a `CheckConstraint` at that point.

- source_spec: `2-1-the-user-model-carries-the-identity-key.md`
  summary: Two concurrent first authentications for the same subject race on the unique constraint, so one request raises `IntegrityError` instead of resolving to the user the other just created.
  evidence: The unique constraint is the only serialization point and nothing reads the field yet — this story deliberately adds no manager method. The race is therefore not reachable today, but it is created by the constraint this story adds and lands the moment Story 2.4's mapper does `get`-then-`create`. The mapper must use `get_or_create` inside `transaction.atomic()` (or catch `IntegrityError` and re-read) rather than a bare lookup-then-insert. Recorded here so 2.4 inherits the requirement rather than rediscovering it in production.

- source_spec: `2-1-the-user-model-carries-the-identity-key.md`
  summary: An over-length subject is silently stored by sqlite and rejected by PostgreSQL, and unlike `name` the identity key has no boundary test.
  evidence: `tests/integration/test_postgres_schema.py` carries a deliberate boundary/over-length pair for `User.name` precisely because "sqlite ignores a `VARCHAR(n)` length entirely" — the R-5 parity gap. `idp_subject` gets `max_length=255` asserted as a number in `tests/unit/users/test_models.py` and no behavioral counterpart, so a subject longer than 255 characters behaves differently on a developer's sqlite than in the gate, on the one field where truncation is a security event. Not fixed here because writing over-length values is the writer's concern and no writer exists yet; if Story 2.2's claims contract ever designates a composite value (`iss|sub`), 255 becomes tight and this needs both a boundary test and a width decision.

## Deferred from: code review of 2-2-the-claims-contract-is-read-from-the-environment (2026-08-16)

- source_spec: `2-2-the-claims-contract-is-read-from-the-environment.md`
  summary: `is_configured` accepts `staff_group == superuser_group`, so a copy-paste when populating a second environment's secrets silently grants `is_superuser` to every member of the staff group.
  evidence: AD-12 requires `is_staff` and `is_superuser` to be set "each from their own designated group", but `ClaimsContract.is_configured` is defined by the spec as `all(...)` over the four fields being non-empty and nothing checks distinctness. `COMPONENT_STAFF_GROUP=platform-staff` with `COMPONENT_SUPERUSER_GROUP=platform-staff` reports a healthy contract, passes whatever Epic 4 gates on it, and Story 2.5's sync then confers superuser on the staff group. Not fixed here because the spec pins `is_configured` to non-emptiness and Epic 4 Story 4.2 owns the refusal that consumes it; widening the predicate silently would change the contract Epic 4 is written against. Whoever authors refusal condition 5 ("the claims contract is unusable") should decide whether distinctness belongs in that refusal or in a separate `is_usable` predicate.

- source_spec: `2-2-the-claims-contract-is-read-from-the-environment.md`
  summary: `read_group_claim` returns `None` for both "claim absent from a valid token" and "claim present but malformed", so Story 2.5 cannot log the two apart and an operator sees an indistinguishable 401.
  evidence: The story took deliberate care to keep absent (`None`) distinct from present-and-empty (`[]`) because AD-12's 401 rule rests on it, then collapsed a third case into the `None` bucket: a group claim holding an object, a null or a boolean member denies with the same signal as a token that never carried the claim. That is the safe direction for authorization and is why it was implemented this way, but it costs the diagnosis — "group claim `realm_access.roles` present but not a list of names" is exactly the message that turns a 401 into a fixable misconfiguration. Not fixed here because the spec's contract for this function is `list[str] | None` and a richer signal is a return-type change the consumer should specify. Story 2.5 should decide whether it needs a distinct malformed signal before it writes the 401 path.

- source_spec: `2-2-the-claims-contract-is-read-from-the-environment.md`
  summary: `mkdocs build --strict` is not in the gate, so a broken nav entry or dead docs link ships green.
  evidence: `pixi.toml` declares a `docs` task running `mkdocs build --strict`, but `ci`'s `depends-on` is `[precommit, build, typecheck, lint, test-cov]` and no `.github/workflows/` step invokes it. This story added `docs/authentication.md` and a `nav:` entry for it — verified by running `pixi run docs` by hand — but nothing in the gate would catch the next one. Pre-existing rather than caused by this story; adding a step to the gate chain is a change `tests/unit/test_gate_contract.py` asserts and Story 1.1 owns, and it interacts with the known `pixi run ci` environment-layering ambiguity that the `gate` feature exists to resolve.

## Deferred from: inline review of 2-3-the-designated-groups-exist-before-the-first-authentication (2026-08-16)

- source_spec: `2-3-the-designated-groups-exist-before-the-first-authentication.md`
  summary: `0003_provision_designated_groups.reverse()` deletes designated groups by name regardless of whether the forward pass created them, so a rollback destroys a `Group` that predated the migration along with its user memberships.
  evidence: `forward()` distinguishes created from pre-existing rows -- that is what `ProvisionResult.created` vs `.existing` records -- but `reverse()` at `:60-66` consults neither. It reads `settings.CLAIMS_CONTRACT` (live settings, not historical state), builds `names` from the two configured group names, and issues `Group.objects.filter(name__in=names).delete()`. A group an operator created by hand before this migration ran, carrying the contract's name, is deleted by `migrate users 0002`, and its `auth_user_groups` rows cascade with it. Two related consequences from the same root cause: reverse reads the *current* contract, so if the configured names changed after forward ran it deletes the wrong rows and leaves the originals orphaned. The docstring's claim -- "Remove only the group rows the contract names, and only those" -- is accurate about names and silent about provenance. Not fixed here because a correct fix needs provenance the story did not design for (recording what forward created, or refusing to delete a group with members), which is a change to AC #1's shape rather than a patch. `test_the_migration_reverse_removes_the_designated_groups` now covers the intended path and pins a bystander group as untouched, so the behaviour is at least visible.
  status: open

## Deferred from: review of 2-4-the-mapper-resolves-an-identity-to-a-user (2026-08-16)

- source_spec: `2-4-the-mapper-resolves-an-identity-to-a-user.md`
  summary: `resolve_user` returns a deactivated user identically to an active one, and the mapper's docstring — otherwise exhaustive about what resolution does and does not do — is silent on `is_active`, so Stories 2.6 and 2.7 have no stated contract to code against.
  evidence: Two independent reviewers raised it. `resolve_user` does `filter(idp_subject=subject).first()` and returns the row unconditionally; nothing consults `is_active`, and no test covers a deactivated identity. For the interactive flow Django's `ModelBackend.user_can_authenticate` still gates the login, so the operator expectation "deactivating a user in the admin locks them out" holds there by accident of the backend rather than by anything this mapper does. The Bearer path (Story 2.7) has no such backend in front of it: a DRF authentication class that calls `resolve_user` and returns the result authenticates a deactivated principal on every API request. Not fixed here because the placement is a genuine decision, not an omission — refusing inside the mapper makes `is_active` an authorization input on the hot path shared by all three callers, while refusing in each caller keeps the mapper protocol-free per FR-8 but must then be written three times and can be forgotten once. Story 2.7 must not be written before this is decided; whichever way it goes, the mapper docstring should state it explicitly so the choice is visible rather than inherited.
  status: resolved (2026-08-17) — decision recorded below; implementation lands with Story 2.7
  resolution: **The refusal lives inside `resolve_user`.** It raises `ClaimsRejected` with its own reason for a user whose `is_active` is False, and every caller inherits it.

  Three arguments decided it. First, the objection this entry raised against mapper placement — that it makes `is_active` an authorization input on the hot path — does not survive contact with the code: `resolve_user` has already loaded the row by that point, so reading `user.is_active` costs zero additional queries. The concern was real in the abstract and empty here. Second, FR-8's "protocol-free" constrains HTTP, not access decisions; the mapper already refuses claims and already decides access in `sync_authorization`, and `ClaimsRejected` is the existing vehicle each caller already translates (403 interactive, 401 Bearer). Third, per-caller placement fails by omission, and the omission has already happened once: Story 2.6 shipped with no explicit check, safe only because allauth's `perform_login` happens to gate inactive users after `pre_social_login` returns — a guarantee no test pins and nothing would notice losing.

  Two constraints on the implementation, both following established precedent. Reuse `ClaimsRejected` with a distinct reason string rather than adding an exception type — Story 2.5 set that precedent for its third refusal and `exceptions.py` is written for it; a deactivated user's claims are perfectly valid, so a shared reason would misattribute an operator action to a bad token. And reword `resolve_user`'s docstring at `mapper.py:191-193`: "Resolution is by the identity key alone" is about which claim *identifies* the user — email and username never consulted — so it needs restating, not reversing, to say that a resolved row must also be active.

  Note that this changes behaviour Story 2.6 already shipped: a deactivated user currently receives allauth's own inactive handling and afterwards receives the adapter's 403. That is the intended outcome, but it must be a deliberate, tested line in Story 2.7 rather than a silent side effect — and the test that pins the interactive path is the one that closes the accidental-gate hole above.

## Deferred from: inline review of 2-6-a-person-authenticates-interactively-against-the-idp (2026-08-17)

- source_spec: `2-6-a-person-authenticates-interactively-against-the-idp.md`
  summary: `tests/unit/test_settings.py::_evict_settings_modules` re-imports `config.settings.base` without restoring the global structlog configuration, so any future `capture_logs()` assertion collected after it goes silently blind rather than failing loudly.
  evidence: `config/settings/base.py` calls `configure_structlog()` at module scope — a process-wide `structlog.configure(..., cache_logger_on_first_use=True)`. A module-level logger bound after that call freezes its processor chain, and `structlog.testing.capture_logs()` then captures nothing while the code under test still logs normally: the assertion reads `len(events) == 0` and the test fails with no hint of the cause. Story 2.6 hit exactly this through `tests/integration/test_site_migration.py` and it took three of Story 2.5's refusal-reporting tests down with it; that file now saves `structlog.get_config()` and restores it in a `finally`, and the same two lines belong in this fixture. Not fixed here because `_evict_settings_modules` is autouse across the whole of `test_settings.py` — a file this story already extended by 137 lines — and it is latent rather than live: `tests/unit/test_settings.py` sorts *after* `tests/unit/authorization/`, so nothing capture-based currently runs downstream of the leak. It becomes live the moment a capture-based test is added anywhere that sorts after it, and the failure mode is a wrong-looking assertion in an unrelated file. The durable fix is arguably neither fixture but `configure_structlog()` growing a documented save/restore helper for tests to use.
  related: This is the **same root cause** the Story 1.2 review recorded on 2026-08-15 ("Importing `config.settings.base` calls the process-global `configure_structlog()`"), which judged it invisible-but-harmless and deferred it to the observability work. Story 2.6 is that prediction coming true. Treat the two as one item with one fix — the shared helper — rather than patching re-importers one at a time; this entry exists to name the one call site still unpatched, not to propose a second solution.
  status: open

## Deferred from: review of 2-7-an-api-client-authenticates-programmatically-against-the-idp (2026-08-17)

- source_spec: `2-7-an-api-client-authenticates-programmatically-against-the-idp.md`
  summary: `COMPONENT_OIDC_AUDIENCE` defaults to the OIDC client id, which is also what an IdP puts in an **ID token**'s `aud` — so an ID token minted for the interactive flow verifies as an API credential on the Bearer path.
  evidence: `src/config/settings/base.py` sets `OIDC_AUDIENCE = env.str("COMPONENT_OIDC_AUDIENCE", default="") or OIDC_CLIENT_ID`, and `authentication.py` passes it to `jwt.decode` as `audience=`. An OIDC ID token asserts `aud = <client_id>` by specification, carries `iss`, `exp` and a signature from the same JWKS, and — once Story 2.5's rule is satisfied by its `jti` — passes every check this class makes. The two credentials are then interchangeable, which is exactly the conflation `at+jwt` (RFC 9068) exists to prevent. Exposure today is bounded: the ID token is delivered to the component's own backend during allauth's authorization-code exchange rather than to the browser, and its holder is the same principal the access token would resolve to, so this is a credential-hygiene defect rather than a privilege escalation. Not fixed in this pass because every available discriminator is a policy choice the spec does not authorize and no AC requires — `typ: at+jwt` is not universally emitted, a `nonce`-presence test is heuristic, and requiring a separate resource-server audience breaks the single-client deployment the default exists to serve. The decision belongs with whoever owns the IdP contract; note that Epic 3 Story 3.5 mints a local token against this same class and would have to satisfy whatever is chosen.
  status: open

- source_spec: `2-7-an-api-client-authenticates-programmatically-against-the-idp.md`
  summary: `JWKSKeyStore._refresh` performs the outbound JWKS fetch while holding the store lock, and nothing bounds the fetch's **total** duration — only its inter-byte idle time and its body size — so a slow-drip response still stalls every concurrent Bearer request in the process.
  evidence: The fetch is inside `with self._lock:` deliberately (AD-23 and the spec's Task 1 both require one fetch rather than N under concurrency), justified in the docstring by `_FETCH_TIMEOUT = (5.0, 5.0)`. That is a `requests` connect/read pair, and its read half is an inter-byte timeout: a server emitting one byte every four seconds never trips it. This review's P6 added `allow_redirects=False` and a 1 MiB streamed body cap, which bounds the worst case to roughly `cap / chunk × 5s` rather than to infinity — a large improvement, not a bound anyone would choose. The remaining fix is a total-deadline around the fetch (a wall-clock budget checked between chunks, or moving the fetch outside the lock behind a single-flight token) and is a design change to the concurrency shape the spec prescribed, not a patch. Threat model is narrow — the JWKS host is the operator-configured IdP origin, so this needs a compromised or degraded IdP, or an on-path attacker — and no test exercises concurrency at all, which is the other half of the gap.
  status: open

- source_spec: `2-7-an-api-client-authenticates-programmatically-against-the-idp.md`
  summary: `jwt.PyJWKSet.from_dict` is not total in at least three ways that escape `PyJWKSetError`, so any future direct call to it outside `_index_by_kid` reintroduces an unauthenticated 500.
  evidence: Found while patching. `{"keys": ["x"]}` and `{"keys": [null]}` raise `AttributeError`; a member missing the field its `kty` requires (e.g. `{"kty": "oct"}`) raises `KeyError`. None of the three is under `PyJWKSetError`, which is the only failure PyJWT documents. `_index_by_kid` now translates all of them, so the Bearer path is covered and pinned by `test_a_keys_array_holding_a_junk_element_is_a_failed_fetch_not_a_crash` — but the translation lives at one call site rather than in a shared helper, and nothing prevents the next caller (Epic 3's Story 3.5 local-token work is the likely one) from calling `from_dict` directly and inheriting the crash. The durable fix is a single parsing helper this package exports, or an upstream issue.
  status: open

- source_spec: `2-7-an-api-client-authenticates-programmatically-against-the-idp.md`
  summary: `test_the_authentication_class_holds_no_mapping_logic_of_its_own` enforces AC #2 by scanning `authentication.py`'s source for literal substrings, which is brittle in both directions and now constrains unrelated edits to that module.
  evidence: The test reads the module source and asserts `"Group"`, `"is_staff"` and `"is_superuser"` do not appear — splitting its own literals (`"is_" + "staff"`) so it does not match itself, which is the technique admitting it is fragile. False positives: any future docstring, comment or third-party import containing "Group" fails the build for reasons unrelated to mapping logic, and this pass already added a drf-spectacular extension to that file. False negatives: `user.groups.add(...)`, a group name read from `settings`, or a claim key held in a variable all pass. The behavioural half of AC #2 is pinned properly by `test_a_valid_bearer_token_authenticates_and_resolves_the_mapped_user`; the structural half wants an AST check for attribute access and imports, or an assertion over the module's call graph, rather than a substring scan. Kept as-is this pass because it does currently hold and replacing it is test-design work with no behaviour change behind it. The same technique is used by Story 2.6's equivalent assertion, so a fix should cover both.
  status: open

- source_spec: `2-7-an-api-client-authenticates-programmatically-against-the-idp.md`
  summary: The structlog save/restore gap in `tests/unit/test_settings.py::_evict_settings_modules` — already open from the Story 2.6 review — was enlarged rather than closed by this story, which added further `config.settings.base` re-imports to that file and a `capture_logs()`-based assertion elsewhere in the suite.
  evidence: This is the **same item** recorded above under "inline review of 2-6", not a new one; it is named again only because this story changed its blast radius. `_evict_settings_modules` is autouse across `tests/unit/test_settings.py`, which this pass extended by roughly 230 lines including five more `importlib.import_module(BASE)` calls, each re-running the process-global `configure_structlog()`. It stays latent purely because `tests/unit/test_settings.py` sorts after `tests/unit/authorization/`; this story added `capture_logs()` assertions to the mapper and jwks suites, which sort before it, so the ordering that keeps it invisible is now load-bearing on more tests than before. Do not fix it twice — the durable fix named on the 2.6 entry (a documented save/restore helper on `configure_structlog()`) closes this one too.
  status: open

- source_spec: `2-8-the-static-token-credential-surface-is-removed-entirely.md`
  summary: No test covers `User.delete()` against a database that still holds the residual `authtoken_token` foreign key, which is the one way the retained residue can break a live deployment.
  evidence: `rest_framework/authtoken/migrations/0001_initial.py:17` declares `user = OneToOneField(settings.AUTH_USER_MODEL, related_name="auth_token", on_delete=CASCADE)`, so an already-migrated database keeps a DB-level FK on `authtoken_token.user_id` after this story removes the app from `INSTALLED_APPS`. Django's deletion collector no longer knows to cascade it, so deleting a user who still has a token row fails on the constraint — reachable from `src/django_service/users/admin.py` (`UserAdmin` inherits the default delete action with no `has_delete_permission` override) and from any `User.delete()` call. Every test database is built fresh from the post-removal app set, so the residual table is never present in any test run and no assertion can currently see this. `docs/authentication.md` now warns the operator about it under "Retired surfaces", which is the right home for the decision, but the warning is prose with nothing pinning it. A test would have to fabricate the residue with raw DDL inside a rolled-back transaction — `tests/integration/test_postgres_schema.py` already drives raw DDL that way and is the natural host. Deferred rather than patched because this repository never creates that table, so the test would be asserting against a state only a deployment history produces.
  status: open

- source_spec: `2-8-the-static-token-credential-surface-is-removed-entirely.md`
  summary: The `KEY_STORE._fetch` stub seam is now duplicated across two integration modules with no shared owner, so the three `# noqa: SLF001` private-attribute reaches must be kept in step by hand.
  evidence: `tests/integration/test_credential_surface.py`'s `_idp` fixture is a copy of `_bearer` in `tests/integration/authorization/test_bearer_authentication.py:95-111` — same `KEY_STORE.reset()` / save `_fetch` / install `StubFetch` / restore sequence, same three `noqa` comments, plus copies of `published_key` and a `claims()` builder. The copy had already drifted on arrival: it omitted the `JWKS_TTL_SECONDS` and `JWKS_MIN_REFETCH_SECONDS` pins that make the original independent of the developer's shell (patched this pass, but the drift is the point). `tests/jwt_keys.py` already exists as the shared home for `SigningKey`, `StubFetch`, `generate` and `jwks_document`; the fixture belongs beside them or in `tests/integration/conftest.py`. Not patched this pass because hoisting it means editing the Bearer suite, which is out of this story's scope and better done when a third caller appears.
  status: open

- source_spec: `3-1-local-pixi-tasks-declare-themselves-local.md`
  summary: Story 4.1's task list directs a *second* locality reader at `src/config/startup/locality.py` with case-sensitive semantics, contradicting AD-1's single declaration site and disagreeing with `src/config/locality.py` on the same input.
  evidence: `4-1-the-refusal-contract-has-one-home-and-two-evaluation-stages.md:55` instructs "Create `src/config/startup/locality.py` with `is_deployed() -> bool` reading `COMPONENT_RUNTIME` from `os.environ` and returning `False` **only** when the value is exactly `"local"`". Story 3.1's Dev Notes state the opposite — "Epic 4's `src/config/startup/` imports this module rather than re-reading `os.environ`. Do not create a second reader" — and 3.1's reader normalizes with `.strip().lower()`. The two would disagree on `COMPONENT_RUNTIME=Local` or `" local "`: local to one reader, deployed to the other, so Story 3.4's URL gating and Epic 4's refusals would reach opposite conclusions about the same process. Pre-existing in 4.1's frozen spec, surfaced by 3.1's review rather than caused by it. Fix belongs in 4.1's spec before that story is driven.
  status: open

- source_spec: `3-1-local-pixi-tasks-declare-themselves-local.md`
  summary: `docs/development.md:54-56` states a deployment runs `manage`, `migrate`, `collectstatic`, `createsuperuser` and `serve`, which contradicts AD-13's premise that a deployed container invokes no local task.
  evidence: The sentence is pre-existing and untouched by story 3.1: "Operational commands -- `manage`, `migrate`, `collectstatic`, `createsuperuser`, `serve` -- run in `default`, because a deployment runs them too." It is corroborated by story 5.5 lines 71 and 77 ("the release stage runs `pixi run migrate`"). Whatever resolution the 3.1 intent gap receives, this paragraph either states the truth that invalidates AC #2's premise or is itself stale and must be corrected -- it cannot stay as written alongside a claim that the local declaration is inert in deployment. Recorded separately because the docs sentence outlives story 3.1 and is load-bearing for Epic 5's release-stage design.
  status: open

- source_spec: `3-1-local-pixi-tasks-declare-themselves-local.md`
  summary: Once the test tasks declare `COMPONENT_RUNTIME=local`, no test in `pixi run ci` ever executes under the ambient deployed default, so a defect in the deployed branch of locality-dependent code cannot fail the gate.
  evidence: `test`, `test-integration` and `test-cov` all carry the declaration, and `ci` reaches `test-cov` through `depends-on` (verified on pixi 0.70.2 that a dependency task keeps its own `env`), so the GitHub runner declares itself local for the entire suite. Deployed behaviour becomes reachable only through an explicit `monkeypatch.delenv`, which today exists in exactly one autouse fixture local to `tests/unit/test_locality.py`. This is a deliberate necessity -- story 4.1 depends on the suite not tripping the deployed refusals -- but it is a real narrowing of what the gate can catch and it appears in no risk register. It belongs beside R-3 in the spine's Named Residual Risks, or needs a deliberate deployed-branch test seam (a subprocess import with the variable scrubbed).
  status: open

- source_spec: `3-1-local-pixi-tasks-declare-themselves-local.md`
  summary: Stories 4.1 and 5.2 carry instructions written against the superseded per-task AD-13 and will fail Story 3.1's gate test if followed literally.
  evidence: AD-13 was amended on 2026-08-17 (spine commit `d40b684`) so that locality is declared once in `[feature.dev.activation.env]` and no task declares `COMPONENT_RUNTIME`; `tests/unit/test_locality_declaration.py::test_no_task_declares_component_runtime` enforces it. `4-1-the-refusal-contract-has-one-home-and-two-evaluation-stages.md:150` instructs the implementer to "add `env = { COMPONENT_RUNTIME = \"local\" }` to the three test tasks" if Story 3.1 has not landed — 3.1 has landed, so the conditional is moot, but an implementer following the literal text breaks the gate. Its accompanying claim that the test suite would otherwise refuse at collection is also now handled by the dev environment's activation env. `5-2-the-process-model-is-declared-as-pixi-tasks-with-its-constraints-as-data.md` asserts the superseded blanket prohibition ("no `COMPONENT_*` in `[activation.env]`") in `tests/unit/test_process_model.py`, which contradicts the permitted `[feature.dev.activation.env]` declaration; its `COMPONENT_PROCESS` half is unaffected and still absolute. The equivalent stale instructions in epic 3 (stories 3.3 and 3.5) were corrected in place when the epic was driven. Deferred rather than patched because both stories are outside the epic being driven and editing an undriven story's spec from another epic's session invites a conflicting decision later.
  update (2026-08-28): the **5.2 half is discharged**. The story's spec was reconciled against `ad3a4aa` before it was driven, and the superseded blanket prohibition was rewritten: `tests/unit/test_process_model.py` asserts neither form of it and instead names `tests/unit/test_locality_declaration.py` as the owner of both the activation-env prohibition and the two task-`env` assertions, which went live with the tasks this story added. The 4.1 half is closed by that story having already landed. This entry stays open only as the record for any remaining spec written against the pre-amendment AD-13.
  status: open

- source_spec: `3-4-local-sign-in-is-a-url-route-that-drives-the-real-mapper.md`
  summary: The refused-claims response re-renders the index at the `POST` URL, where a browser refresh answers 405 rather than the page again.
  evidence: `src/config/local_dev/views.py` returns `_render_index(..., status=400)` from `persona_signin`, so the 400 body is served at `/_local/<persona>/`. That path is `POST`-only, so refresh or back-forward issues a `GET` and gets `HttpResponseNotAllowed`. By `docs/development.md`'s own account this 400 is the *first* thing a developer sees on a fresh clone with no `COMPONENT_IDENTITY_CLAIM` configured, so the page most likely to be reloaded is the one that breaks on reload. Not patched here because the fix is POST/redirect/GET through `django.contrib.messages`, which is a wider change than this story's shape and would put the rejection reason into session state; the current behaviour is ordinary Django form-error behaviour and is correct, just awkward at exactly one moment.
  status: open

- source_spec: `3-4-local-sign-in-is-a-url-route-that-drives-the-real-mapper.md`
  summary: Nothing verifies the claim, stated in three docstrings, that a deployed component never imports `config.local_dev.urls`.
  evidence: `src/config/urls.py`, `src/config/local_dev/urls.py` and `tests/unit/test_local_dev_urls.py` all rest on the dotted-string `include()` inside `local_signin_urlpatterns()` deferring the import, and that deferral is the entire justification for the string form over a module reference. `test_the_mount_is_absent_when_the_run_is_not_local` only checks the returned list, in a process where `config.local_dev.urls` is already in `sys.modules` from the local branch, so it cannot distinguish deferred from eager. Asserting it needs a subprocess that imports `config.urls` with `COMPONENT_RUNTIME` scrubbed and inspects `sys.modules` — the same deployed-branch test seam another open entry above already asks for, which is why it is recorded rather than built twice.
  status: open

- source_spec: `3-5-the-local-programmatic-flow-validates-for-real.md`
  summary: When Epic 4's stage-1 trust-anchor refusal lands, the local `file://` JWKS location will fail its own predicate, and the refusal must exempt a local run or local development refuses at boot.
  evidence: `config/settings/local.py` sets `OIDC_JWKS_URL` to a `file://` URI and `OIDC_ISSUER` to `https://local-dev.invalid/realms/component`, and `jwks_url_derives_from_issuer` answers `False` for that pair by construction — no `file://` string derives from an `https://` origin. That is exactly the state AD-23's stage-1 condition refuses, and it is permitted here only because locality is local. Story 4.2 must therefore scope the trust-anchor refusal to deployed runs; if it is written as an unconditional settings-import refusal, `pixi run -e dev runserver` stops starting on a fresh clone and the first thing a new developer meets is the refusal Epic 3 exists to avoid. The same decision point governs whether `_local_document`'s own `is_local()` gate stays as defence in depth or is removed as redundant once the startup refusal exists — it should stay, because the two guard different moments (import versus first Bearer request), but that is a call for the story that adds the refusal. Recorded rather than patched because it is an instruction to an undriven story in another epic.
  status: open

- source_spec: `3-5-the-local-programmatic-flow-validates-for-real.md`
  summary: `mint.py`'s `DJANGO_SETTINGS_MODULE` default is unverifiable in-process, so the settings module the task actually runs under is asserted nowhere.
  evidence: `src/config/local_dev/mint.py` opens with `os.environ.setdefault("DJANGO_SETTINGS_MODULE", "config.settings.local")`, and under pytest-django the variable is already set to `config.settings.test`, so the call is always a no-op in the suite and the string could be changed to any module while every case stays green. `src/config/local_dev/seed.py:49` carries the identical unverified line, so this is the established shape rather than something this story introduced. Closing it needs the subprocess test seam two open entries above already ask for — a child process with a scrubbed environment that imports the entry point and reports what settings it resolved — which is why it is recorded once here rather than built a third time.
  status: open

- source_spec: `3-6-observability-is-not-substituted-locally.md`
  summary: `tests/integration/test_local_trace_correlation.py` and `tests/integration/test_asgi_request_path.py` carry byte-identical copies of the `recorded_spans` fixture and the three environment-hint helpers; they belong in `tests/integration/conftest.py`.
  evidence: `SDK_DISABLED_VALUES`, `SUPPRESSING_SAMPLERS`, `_sdk_is_disabled`, `_suppressing_sampler`, `_span_absence_hint` and the whole `recorded_spans` fixture — roughly sixty lines — appear in both files with the same bodies and the same `# noqa: SLF001` justifications. The duplication is deliberate here: Story 3.6's spec said to reuse the idiom "verbatim in shape", and lifting it into `tests/integration/conftest.py` changes a shared file that a second, already-passing module depends on, which is beyond a story that owns only the local non-substitution property. It is worth doing once a third consumer appears, or when Epic 6 (FR-45) adds the collector-stub tests, which will want the same fixture. Note that `_sdk_is_disabled` also re-implements the private `telemetry._is_disabled` in both files, so a change to the accepted truthy values has three sites rather than one.
  status: open

- source_spec: `3-7-nothing-on-the-local-start-path-reaches-the-network-at-boot.md`
  summary: Story 2.7's two "reaches no network" tests guard `requests.get` rather than the socket layer, which is weaker than the `no_network` fixture this story added.
  evidence: `tests/unit/authorization/test_jwks.py:150-175` (`test_importing_the_bearer_modules_reaches_no_network`) re-executes the `jwks.py` and `authentication.py` module bodies with only `requests.get` replaced, and `tests/integration/test_local_dev_bearer_flow.py:274-278` does the same for the `file://` key path. Both would pass if the code under test ever reached for `urllib`, `httpx` or a raw socket instead — the exact substitution the `no_network` docstring argues a library-level guard cannot survive. Requesting `no_network` there is a one-line change per test, but it edits two already-passing Epic 2 modules whose property this story does not own, and `test_importing_the_bearer_modules_reaches_no_network` in particular is load-bearing for AD-23 in a way that deserves its own pass rather than a drive-by. Note that this story's `config.authorization.authentication` module body is still verified only there: the boot probe imports `config.authorization.jwks` but never `authentication`, because `base.py:573` names `OIDCBearerAuthentication` as a dotted string that DRF resolves lazily.
  status: open

- source_spec: `3-7-nothing-on-the-local-start-path-reaches-the-network-at-boot.md`
  summary: The deployed start paths — `config/wsgi.py`, `config/asgi.py` and `config.settings.production` — have no boot probe; only the local path is asserted.
  evidence: `tests/unit/test_no_network_at_boot.py`'s child hardcodes `DJANGO_SETTINGS_MODULE=config.settings.local`, and `test_settings_import_performs_no_network_call` imports only `BASE` and `LOCAL` although `SETTINGS_MODULES` names `PRODUCTION` and `TEST` too. That is correct scoping for this story, whose title is the *local* start path — but NFR-1's "startup makes no network call, and no query beyond migration state" covers the deployed entrypoints, where a boot-time fetch is the one that pages someone. `src/config/settings/production.py` is environment reads today, so this is unverified rather than broken. The probe generalises cheaply: parametrise `_BOOT_PROBE_SOURCE` over the settings module and boot through `get_wsgi_application()` with `DJANGO_SECRET_KEY` and `DJANGO_ADMIN_URL` supplied. It belongs with Epic 4's stage-2 refusal checks (AD-26), which inherit the same constraint and need the same seam.
  status: open

- source_spec: `4-4-two-feature-scoped-refusals-apply-only-where-their-feature-exists.md`
  summary: Stripping a `feature:` region leaves unused imports behind, so a materialized combination fails `ruff check` with `F401` until Epic 8 prunes orphan imports.
  evidence: Rehearsed rather than predicted — all four regions were stripped from the three marker-bearing files in three combinations, and `ruff check --select F` reports seven `F401`s: `DummyCache`, `LocMemCache` and `import_string` in `src/config/startup/stage_one.py`, plus four in `tests/unit/startup/test_feature_scoped_refusals.py`. The imports cannot be marked in place: a marker pair inside the sorted import block does not survive ruff's isort, which merges the comment-separated run back together and carries the markers with whichever import they attach to (probed under `ruff check --fix`). `F` is in `lint.select` with no per-file ignores, so this is a gate failure in the materialized tree rather than a cosmetic leftover. It is the same problem AD-24's own worked example names — `CeleryInstrumentor().instrument()` surviving into a combination whose environment no longer contains the instrumentor — and AD-24 already places that on Epic 8. Recorded here because Story 4.4 is the first code to carry regions at all, so it is the first place the orphan-import cost is measurable rather than hypothetical. Note the blank-line half of the same problem *was* closed here, by moving every marker flush against the code it delimits; all three stripped combinations now pass `ruff format --check`.
  status: open

- source_spec: `4-4-two-feature-scoped-refusals-apply-only-where-their-feature-exists.md`
  summary: Condition 8 refuses `LocMemCache` and `DummyCache` but accepts `FileBasedCache` and `DatabaseCache`, both of which fail in exactly the way its own refusal message describes.
  evidence: `_IN_PROCESS_CACHE_BACKENDS` in `src/config/startup/stage_one.py` holds two entries because Story 4.4's Task 1 named exactly two. The message the condition raises argues an in-process cache "is per-worker and per-container, so a value written by one gunicorn worker is invisible to the next request and gone at the next restart" — which is true of `django.core.cache.backends.filebased.FileBasedCache` on a container's ephemeral disk, and `FileBasedCache` additionally writes to the filesystem against FR-38. The docstring's own justification for including `DummyCache` ("the same forbidden state in the spelling that does not look like it", by analogy with `spatialite` beside `sqlite3` in condition 1) applies unchanged to `filebased`. Not patched in Story 4.4 because the refusal table is settled at nine conditions across fourteen forbidden states in `epics.md:310-328`, and widening what condition 8 refuses is a change to that settled contract rather than a defect in the implementation of it — the story implemented precisely what it specified. It belongs with whoever owns the refusal table; Story 4.5 audits that all fourteen states are covered and is the natural place to decide whether this is a fifteenth or a widening of the eighth.
  status: open

- source_spec: `5-1-the-component-declares-itself-in-component-toml.md`
  summary: Nothing reconciles `selected_features` against the `[[processes]]` entries, so a half-stripped AD-24 region — the feature line removed and the process entries left, or the reverse — loads clean.
  evidence: `component.toml` carries two independent `# feature:celery` regions (the `selected_features` line at `component.toml:91-93` and the `worker`/`beat` entries at `component.toml:151-168`), and the loader validates each half in isolation: a declaration omitting `celery` while still declaring `worker` and `beat` is accepted. Story 5.2's two-way gate test reconciles processes against *pixi tasks*, not against the feature list, so no story currently owns the cross-region consistency. Not fixed here because the obvious loader check needs a process-name-to-feature mapping (`{"worker","beat"} ⟹ celery`) that this story is not the authority for — Story 5.2 owns the process names and Epic 8 owns the strip itself. Deciding where the check lives is the open question; a materializer-side post-strip re-parse would catch every region rather than this one.
  status: resolved (2026-08-28) -- Story 5.2 took it, since that is where the missing piece arrived: the story owns the process names, so the `{"worker", "beat"} implies celery` mapping is declared once in `tests/unit/test_process_model.py` and `test_the_celery_feature_its_processes_and_its_task_region_are_present_or_absent_together` reconciles all three regions -- `selected_features`, the `[[processes]]` pair and the new `# feature:celery` region in `pixi.toml` `[tasks]` -- as one signal that must be wholly present or wholly absent. The check is in the gate rather than in the loader, so it also covers the `pixi.toml` region the loader cannot see. Epic 8's materializer-side post-strip re-parse remains the broader answer and is unaffected.

- source_spec: `5-2-the-process-model-is-declared-as-pixi-tasks-with-its-constraints-as-data.md`
  summary: Six assertions in `tests/unit/test_component_declaration.py` are pinned to the reference combination, so they fail in any combination that strips a `# feature:` region — which is every combination Epic 8's per-combination gate runs.
  evidence: Rehearsed rather than predicted. Stripping the `# feature:celery` regions from both `pixi.toml` and `component.toml` and running the four manifest-reading modules leaves `tests/unit/test_process_model.py`, `tests/unit/test_locality_declaration.py` and `tests/unit/test_gate_contract.py` green and fails six cases in `tests/unit/test_component_declaration.py`: `test_selected_features_is_the_reference_combination`, `test_the_loader_reads_the_repositorys_own_declaration`, `test_the_process_group_is_exactly_web_worker_and_beat`, `test_beat_is_a_single_replica_replaced_before_it_is_started`, `test_the_celery_processes_sit_inside_a_marker_pair` and `test_each_selected_feature_sits_on_its_own_marked_line`. That pinning was deliberate in Story 5.1 — its review hardened those very tests against literals precisely so a widened constant could not pass them — and it is correct for the reference application. It is not correct for a `core` test module, which travels into all six combinations and must hold in each. Story 5.2 hit the same shape in its own region test and fixed it there (absence of the region asserts the complement instead of raising), so the pattern is settled; applying it to the six above is Epic 8's, alongside Story 8.8's full gate per combination, because the fix needs the materialized-combination fixture set (Story 8.6) to be verified rather than argued. Recorded here rather than patched in Story 5.1's file because deciding *which* of the six become combination-aware and which move to a reference-application-only module is a scoping call Epic 8 owns.
  status: open

- source_spec: `5-1-the-component-declares-itself-in-component-toml.md`
  summary: `component.toml` is declared `core` and "always travels", but it is in neither build target, so a component installed from a built distribution cannot resolve its own declaration.
  evidence: `[tool.hatch.build.targets.wheel] only-include = ["src"]` and the sdist `include` list (`pyproject.toml:192-204`: src, tests, manage.py, mkdocs.yml, docs, README.md, LICENSE) both omit `component.toml`. Verified against the current `dist/` artifacts: the wheel's root entries are `config/*` and `django_service/*` only, and the sdist root carries no `component.toml` and no `pixi.toml`. From an installed layout `Path(__file__).resolve().parents[3]` (`src/config/component/loader.py:91`) points above `site-packages`, so `load_component_declaration()` raises `ImproperlyConfigured`. The gate is green because the project runs from the tree under an editable install. Not fixed here: adding the file to the sdist would not make the wheel path work — `sources = ["src"]` means a wheel-installed `config/component/loader.py` is four levels below `site-packages` regardless — so this is a packaging design decision rather than a one-line include. AD-28's "always travels" is about materialization, not Python packaging, so the two readings need reconciling. `docs/deployment.md` now states the limitation. Belongs with Story 5.6, where the component is a payload.
  status: open

- source_spec: `5-1-the-component-declares-itself-in-component-toml.md`
  summary: `pixi run docs` (`mkdocs build --strict`) is in no CI job and in no `ci` dependency chain, so the documentation set is never built, link-checked or rendered by any gate.
  evidence: `[feature.gate.tasks] ci` (`pixi.toml:548-556`) chains precommit, build, typecheck, lint and test-cov; `docs` is absent. Grepping `.github/workflows/` for `pixi run` finds `ci`, `test`, `test-integration`, `ruff-report`, `changelog`, `migrate` and `runserver` — never `docs`. The consequence is concrete rather than theoretical: this story's Dev Notes and the new test's docstring both justify registering `docs/deployment.md` in `mkdocs.yml` `nav` on the grounds that "`mkdocs build --strict` fails on a page absent from `nav`", which is true of the task and false of the gate. A unit test now asserts the nav registration, so the page is not unguarded — but nothing builds it, and a broken reference or a malformed fence in any docs page ships silently. Pre-existing and project-wide: `docs/` has held four pages since before this story. Adding `docs` to the `ci` chain is one line, but it moves an mkdocs build into every gate run and belongs with whoever owns the gate's cost budget.
  status: open

- source_spec: `5-1-the-component-declares-itself-in-component-toml.md`
  summary: `tests/unit/startup/test_installed_apps_ordering.py` states an obligation this story's landing was supposed to trigger, and it was neither discharged nor recorded.
  evidence: That module's header says verbatim that "the adopted-app list arrives with `component.toml` (Story 5.1)" and that "when `component.toml` lands, this module gains the index comparison against its adopted-app list", keeping its current assertions as the structural backstop. `component.toml` has landed with `adopted_apps = []` and the module is untouched by this diff. Defensible to defer — an empty list makes the index comparison assert nothing today — but Epic 9 is where `adopted_apps` becomes non-empty (AD-8's composition step), and an obligation recorded only in a docstring of a passing test is one nothing will surface at that moment. Discharge it with the Epic 9 story that adds the first adopted app, or sooner with a comparison written to be vacuous-but-present against the empty list.
  status: open

- source_spec: `5-1-the-component-declares-itself-in-component-toml.md`
  summary: The spine's AD-24 region table records one marked region for `component.toml`; the implementation carries four, and Epic 7's `[[regions]]` declaration will be written from the spine.
  evidence: `ARCHITECTURE-SPINE.md` AD-24 lists for `component.toml` exactly "the `worker`/`beat` replica and replacement constraints". `component.toml` now carries that region (`component.toml:151-168`) plus one marker pair per `selected_features` entry (`component.toml:91-99`) — four pairs in total. The story authorized the per-line form explicitly and the file's own comments flag it as an open question for Epic 7/8, but the record lives in the story file and in a TOML comment, not in the spine Epic 7 reads when it authors the carrier's open `[[regions]]` array. AD-24's region set is open and encodes no count, so nothing is contradicted — the risk is an omission at declaration time, where an undeclared region is one the materializer never strips.
  status: open

- source_spec: `_bmad-output/implementation-artifacts/5-7-sessions-are-database-backed-with-the-engine-set-explicitly.md`
  summary: The prune admin process issues one unbounded `DELETE ... WHERE <expiry> < now` per leg, with no `LIMIT` and no chunking, so a first run against a long-neglected table can hit a `statement_timeout` and roll back without making any progress — identically on every retry.
  evidence: `src/django_service/users/management/commands/prune_expired_state.py` deletes whole querysets; `docs/deployment.md` now warns about it rather than fixing it. A `--batch-size` loop is a design decision (it changes the process from one statement to many, and the count it reports from exact to cumulative) that Story 5.7's ACs do not cover.

- source_spec: `_bmad-output/implementation-artifacts/5-7-sessions-are-database-backed-with-the-engine-set-explicitly.md`
  summary: `CredentialEpoch` rows with a NULL `expires_at` are pruned by nothing, ever, so that row class grows without bound in every combination.
  evidence: `config.authorization.mapper._expires_at` writes `None` for any token with no readable `exp`, and the pruner's `expires_at__lt` correctly excludes NULLs — removing them early would re-sync a live credential. Bounding them needs a policy nobody has taken (an age cap on `first_seen_at`, or the mapper writing a fallback expiry), which is AD-10's question rather than AD-31's. `docs/deployment.md` records the residue.
